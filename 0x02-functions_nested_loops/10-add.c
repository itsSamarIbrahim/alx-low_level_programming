#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 *
 * @x: the first number
 * @y: the second number
 *
 * Return: 0
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
