#include "main.h"

/**
 * _islower - a function that checks for lowercase character. 
 *
 * return : 0
 */
int _islower(int c)
{
	char letter;

	if (_islower(letter = c))
		return (1);
	else
		return (0);
}
