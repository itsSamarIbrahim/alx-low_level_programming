#include "main.h"

/**
 * _isupper - a function that checks for uppercase character.
 *
 * Return: 1 if c is uppercase or 0 otherwise
 */
int _isupper(int c)
{
	char c

	if (isupper(c))
	{
		return (1);
	} else
	{
		return (0);
	}
}
